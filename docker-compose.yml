services:
  web:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER}/imposter-game-web:latest
    ports:
      - "3010:80"
    depends_on:
      - server
    restart: unless-stopped

  server:
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER}/imposter-game-server:latest
    environment:
      - DATABASE_URL=file:/data/imposter.db
      - NODE_ENV=production
      - PORT=3001
    volumes:
      - db_data:/data
    restart: unless-stopped

volumes:
  db_data:
